---
id: 001
title: Bootstrap incus-oci project
started: 2026-08-06
---

## 2026-08-06 16:10 — Kickoff
Goal for the session: not yet stated in detail; the user opened a session immediately after bootstrapping the repo, so the working assumption is initial development of `incus-oci`.
Current state of the world: repo `componere/incus-oci` freshly created from the `meigma/template-go` template (default branch `master`, single "Initial commit"). Session framework set up: journal branch `journal/jmgilman` created and pushed, `.journal/` scaffolded. Template ships mise/moon tooling, melange/apko image pipeline, and repo skills under `.agents/skills`. No project code written yet; `DELETE_ME.md` template placeholder still present.
Plan: await the user's actual request for this session.

## 2026-08-06 16:14 — Goal stated: build familiarity
User defined the project space: `incus-oci` aims at unified management of container and disk images, with Incus (specifically IncusOS) as the primary virtualization layer and bootc-generated images as the focus. Envisioned flow: dual-variant Containerfile (normal + bootc) → podman build/publish to registry → image-builder produces QCOW2 wrapped with Incus metadata → QCOW2 is a one-time boot artifact; subsequent upgrades happen in-place via standard bootc process. Instruction: build familiarity only — no design, no strong opinions yet. Launched four parallel research agents: (1) bootc + bootc-image-builder, (2) Incus image format/VM model, (3) IncusOS, (4) ecosystem prior art (OCI artifacts, containerDisk, simplestreams, etc.).

## 2026-08-06 16:35 — Research sweep complete (4 agents)
Key facts gathered (details in agent reports, distilled here):
- bootc: OCI image + kernel at /usr/lib/modules; ostree/composefs backend; CNCF sandbox, weekly releases (v1.16.7). /etc 3-way merge, /var machine-local, /usr read-only. upgrade/switch/rollback track a registry ref. Logically bound images via Quadlet for app payloads.
- bootc-image-builder ARCHIVED June 2026 → merged into unified osbuild/image-builder CLI (`image-builder build qcow2 --bootc-ref=...`); container wrapper remains compatible for now. podman-bootc also archived → replaced by bcvk (ephemeral VMs, to-disk, libvirt).
- Incus images: split format = metadata tarball (metadata.yaml: architecture + creation_date + optional properties/templates) + disk.qcow2 for VMs. `incus image import meta.tar.gz disk.qcow2 --alias x`. Fingerprint = sha256(meta+data). VMs: UEFI default, security.secureboot default TRUE (MS keys), csm option. incus-agent via 9p/virtiofs config drive or `agent:config` ISO; needed for exec/file ops; console works without. cloud-init only in /cloud variants; agent-less path = `cloud-init:config` NoCloud seed ISO.
- simplestreams: static HTTPS tree; incus-simplestreams CLI (add/list/remove/generate-metadata); qcow2 published as disk-kvm.img.
- Incus OCI app containers since 6.3 (skopeo+umoci), containers only; open issue #1360 for OCI-as-VM.
- IncusOS: GA Nov 2025, Debian 13 + mkosi, A/B sysupdate, Secure Boot + TPM required, ZFS, no shell — API only via incus-osd; apps as signed sysext (Incus, Operations Center, Migration Manager). FuturFusion stack rebrands it HypervisorOS. Incus 7.0 LTS May 2026.
- Prior art bootc+Incus: essentially greenfield. One forum thread (Feb-Mar 2026): bib qcow2 fails Incus import — sgdisk --move-second-header exit 4, GPT layout issue, upstream bug bootc-image-builder#1195. No dedicated projects exist.
- Registry-side disk shipping: OCI 1.1 artifacts (artifactType/Referrers), ORAS, podman artifact stable since 5.6, podman machine distributes its own qcow2 via OCI artifacts, KubeVirt containerDisk (/disk/ in scratch image) as closest analog.
- First-boot config landscape: cloud-init (Incus-native keys), Ignition, systemd credentials via SMBIOS (Incus only through raw.qemu overrides).
Next: awaiting user direction; design phase comes later.

## 2026-08-06 16:58 — New goal: spike Containerfile → Incus bootc QCOW2
Goal directive: prove one or more viable shapes for Containerfile → bootc QCOW2 → Incus import, empirically, no hacks; compare all viable methods. Full autonomy for podman/OrbStack/Lima locally; ask before installing host tools. rk1 remote is dead — local only.
Environment: macOS 26.4, M4 Max (nested virt capable), podman 5.7.1, lima 2.0.3 (vz+nested), incus client 7.2, qemu-img 10.2, orbstack, docker, oras. No skopeo on host.
Planned methods: A) bootc-image-builder container B) unified image-builder --bootc-ref C) bcvk to-disk D) bootc install to-disk --via-loopback. Import via incus image import (split) and incus-migrate. Watch for GPT sgdisk bug (bib#1195).

## 2026-08-06 17:15 — Spike progress: Method A built; import bug reproduced + root-caused
- Task1: minimal Containerfile FROM quay.io/fedora/fedora-bootc:44 (aarch64), root:spike pw; `bootc container lint` 13 checks pass; kernel 7.1.6. Built rootful in podman machine (FCOS 43).
- Method A (bib container quay.io/centos-bootc/bootc-image-builder:latest): FAILS without `--rootfs` on Fedora images ("missing required info: DefaultRootFs" — Fedora sets no default rootfs). With --rootfs ext4: SUCCESS → 1.1GB qcow2 (10GiB virtual) + vmdk/vpc/gce/ovf checksums in manifest.
- Incus host: Lima 2.0.3 vz VM w/ nestedVirtualization=true on M4 Max → /dev/kvm present; zabbly incus 7.3 (server) installed. GOTCHA: `incus admin init --auto` failed "no unused IPv4 subnet" (Lima net density); manual `incus network create incusbr0 ipv4.address=10.171.99.1/24` + profile root disk fixed. GOTCHA: images:fedora/43 VM fails default secureboot (unsigned distrobuilder bootloader, "Access Denied"); security.secureboot=false → boots, agent up, IP on bridge. Nested-virt boot is slow (~60-90s to agent).
- Import: `incus image import metadata.tar.gz spike-bib.qcow2` SUCCEEDS (fingerprint 4b69437e). LAUNCH fails: `sgdisk --move-second-header root.img` exit 4 — bib#1195 reproduced today on latest everything.
- ROOT CAUSE: sgdisk: "Secondary partition table overlaps the last partition by 2014 blocks... Aborting". osbuild GPT ends last partition flush to disk end, no room for backup table where sgdisk wants it; `sgdisk -v` alone reports "No problems, 0 free sectors". Builder-vs-importer strictness collision; disk itself boots in plain qemu.
- Next: incus-migrate path w/ same qcow2; Method D (bootc install to-disk, different partitioner) predicted to differ; B likely same osbuild GPT.

## 2026-08-06 17:50 — Two pipeline shapes proven
- incus-migrate path (Method A artifact): scripted TUI (yes/2/name/path/yes/yes/1) over local server; converts qcow2→raw itself, NO sgdisk step → instance created fine. Secure boot ON: shim loads but next stage fails "Verification failed: (0x1A) Security Violation" — Fedora aarch64 bootc chain NOT bootable under Incus default secureboot. secureboot=false → boots to login, DHCP lease visible in incus list (no agent). GOTCHA: Ubuntu archive incus-tools/incus-migrate packages CONFLICT with zabbly incus (removed the daemon!) — zabbly's is incus-extra. GOTCHA: incus console on migrated VM errors "Chardev user does not support chardev hotswap" (baseline VM consoles fine).
- Method D (bootc install to-disk --via-loopback --generic-image --filesystem ext4, in podman machine, then qemu-img convert): GPT is SANE — 128-entry table, 2015 free sectors after last partition, sgdisk --move-second-header exit 0. incus image import + launch: WORKS, no sgdisk error. Boot slow under nested virt (~4-5min to DHCP). SSH via baked key: bootc status healthy (booted localhost/spike-bootc:latest, ostreeContainer, softRebootCapable), marker file present. **Cleanest shape so far: podman build → bootc install to-disk → qcow2 → incus image import → launch.**
- Root-cause differential: osbuild GPT ends last partition flush at disk end (no backup-table gap) → sgdisk exit 4 at Incus unpack; bootc's own installer leaves standard gap.
- Image rebuilt with SSH key (AuthorizedKeysFile /usr/share/spike/authorized_keys drop-in).
- Next: Method B (unified image-builder CLI, localhost/ib:latest container ready — expect same osbuild GPT), Method C bcvk, maybe bootc-upgrade-under-Incus test + sgdisk -e remediation experiment (labeled as workaround, not pipeline).

## 2026-08-06 18:10 — Method B confirmed same defect; upgrade lifecycle PROVEN; bcvk saga
- Method B (unified image-builder CLI v77.0.0, osbuild 185, fedora:44 container): GOTCHA needs a real volume at /var/cache/image-builder/store (chcon fails on overlay). Produces qcow2 fine — but GPT identical to bib: part3 ends at last usable sector 20971486, sgdisk move-second-header exit 4, "reduce table by 8056 entries". Same osbuild backend = same Incus import incompatibility. Would work via incus-migrate like A.
- Remediation experiment (labeled workaround): sgdisk --resize-table=128 does NOT fix it; overlap persists — real fix needs partition shrink or full GPT rewrite. osbuild artifacts are structurally incompatible with incus image import today.
- bcvk 0.18.0 (Fedora 44 pkg, no aarch64 github binaries): to-disk boots an EPHEMERAL VM (needs /dev/kvm + host qemu) — not a podman-only path. Hidden host deps found empirically: /dev/vhost-vsock, and `objcopy` (binutils) — "Missing required executable: objcopy" only visible by manually running its hybrid container attached (bcvk swallows container stderr). bwrap required in TARGET image (fedora-bootc base ships it). Run in progress on dedicated Fedora 43 Lima VM w/ nested virt.
- UPGRADE LIFECYCLE PROVEN in spike-dvm (Method D image): registry:3 via nerdctl on incus-host (10.171.99.1:5000); insecure registries.conf drop-in written at runtime in /etc (persists via bootc /etc merge); skopeo copy docker-archive → registry (podman save from machine). bootc switch → staged; reboot → booted from registry ref. v2 push (marker change): bootc upgrade pulled ONLY 1 layer/200 bytes; reboot → marker=V2, rollback deployment retained. Full vision validated: one-time qcow2 boot + registry-tracked in-place upgrades + rollback, all inside Incus.
- Nested-virt flakiness: one boot froze in systemd pid1 ("Failed to fork off sandboxing environment: Protocol error"), another had journald timeout with 3 concurrent nested VMs; resolved by force-restart / stopping idle VMs. Not pipeline defects.

## 2026-08-06 19:00 — Spike concluded
- Method B artifact ALSO verified via incus-migrate: spike-bvm boots, SSH bootc status healthy. Matrix cell closed.
- Method C conclusion: bcvk to-disk conclusively blocked on aarch64 today — F44 AND F43 kernels are zstd EFI zboot; host QEMU 10.1 cannot direct-load them; bcvk hangs silently (no console output ever) instead of erroring. Would need newer host QEMU or x86_64 to retest.
- Full findings written to .journal/001/spike/FINDINGS.md (verdict matrix + osbuild GPT root cause + upgrade lifecycle + operational gotchas). Spike source files preserved in .journal/001/spike/.
- Environments left in place: podman machine (images + artifacts in /var/tmp), Lima incus-host (Incus 7.3: spike-dvm running v2, spike-mig/spike-bvm/baseline stopped, registry on :5000), Lima bcvk-host (idle). Scratchpad holds all qcow2s.

## 2026-08-06 19:15 — Post-spike Q&A findings (empirical)
- Raw disk import: `incus image import` AND `incus-simplestreams add` both reject raw ("Unsupported compression" — format sniffer only accepts qcow2 for VMs). Raw is accepted only by incus-migrate (one-off instances, converts client-side). qemu-img convert to qcow2 is unavoidable for image distribution; note Incus converts back to raw internally at unpack.
- Metadata strictness divergence: incus-simplestreams requires properties.architecture; incus image import accepts top-level architecture only. Emit both. With fixed metadata, our metadata.tar.gz + qcow2 pair added cleanly to a simplestreams tree as a virtual-machine product (combined sha256 fingerprint).

## 2026-08-06 19:30 — Ideation: simplestreams proxy architecture
User direction: Incus host → simplestreams URL → base image → normal bootc takeover. Question: proxy builds qcow2 on-demand from bootc image (1) vs pipeline pre-builds qcow2 to registry, proxy streams (2).
My recommendation: (2) build-on-write. Key arguments: simplestreams index publishes sha256/fingerprint/size up front → can't be lazy (builds non-deterministic, fingerprint needs bytes); on-demand proxy = privileged multi-arch build farm in serving path. Refinement: qcow2 needed per tracked ref/channel (bootstrap artifact, refreshed on cadence), NOT per version — bootc upgrade closes first-boot gap (spike: 200-byte delta). Proposed artifact: one OCI artifact, two blobs (metadata.tar.gz + qcow2) mirroring Incus split image, fingerprint/properties as annotations; proxy = stateless OCI→simplestreams translator. Precedent: podman-machine-os. Cost noted: privileged CI runner per arch at publish time.

## 2026-08-06 19:45 — Ideation: large images / single-blob qcow2 concern
Upgrade path immune (layered bootc image, 200-byte delta proven; requires non-squashed Containerfiles — bootc rechunk exists). qcow2 pain: no dedup across refreshes (full multi-GB blob each), first-boot time ∝ full size, registry blob caps (~10GB GHCR). Levers: qcow2 zstd compression (-c, Incus acceptance untested); PRIMARY: generic minimal bootstrap disk per arch/channel + first-boot `bootc switch` to app ref (proven mechanism) → app payload only travels layered path; offer as mode, not mandate. Rejected for now: CDC chunking of qcow2 (layout shifts defeat it; unnecessary if bootstrap stays small). GC policy needed day one.

## 2026-08-06 19:55 — Ideation: near-universal bootstrap image
Sort bootstrap contents by when fixed: (a) install-time irreducibles — storage layout (fs type, LVM/LUKS, /var split), arch, someday SB keys; (b) standardizable machinery — provisioning stack (cloud-init natural given Incus native support); (c) first-boot-deliverable data — target ref, registry auth, CAs, mirrors, sig policy, users/keys/net (cloud-init writes before switch; chicken-egg resolves). End state: small matrix arch × fs-flavor per channel, not per-user images. Two experiments required before believing: (1) /var seeding divergence — switch is an update, image /var unpacks only at initial deploy → bootstrap-then-switch skips app-image /var content (candidate pipeline lint); (2) cross-distro-family switch (Fedora bootstrap → CentOS/Ubuntu-bootc app image).

## 2026-08-06 20:10 — Ideation: incus-oci tool shape
User vision: single binary — (1) Containerfile → published qcow2 in OCI registry, (2) proxy mode serving those as simplestreams. Qemu question resolved: recommend containerized qemu-img for v1 (podman already irreducible hard dep for bootc install → zero added host deps); pure-Go qcow2 writer (raw→uncompressed v3, SEEK_DATA/SEEK_HOLE) viable later upgrade (no mature Go writer exists; would need qemu-img check + incus-import round-trip in CI). Never needs qemu-the-hypervisor.
Flagged: "config.toml → image-builder" implies osbuild backend = Incus-incompatible today (GPT bug). Recommend: disk-builder as swappable port; bootc-install backend default; image-builder backend gated on upstream #1195; fs-size = native flag (truncate); user injection deliberately not replicated (cloud-init at provision; baked users belong in Containerfile).

## 2026-08-06 20:20 — DECISION: bootc install to-disk is the sole disk-build path
User direction: stop referencing spike alternatives; assume `bootc install to-disk --via-loopback` only. Pipeline: podman build → push → truncate + in-image bootc install → containerized qemu-img convert → metadata.tar.gz + qcow2 as one OCI artifact → proxy serves simplestreams. Config surface: Containerfile (incl. /usr/lib/bootc/install TOMLs) + CLI flags (disk size, fs, targets) + cloud-init at provision. No intermediate config file. Host deps: rootful podman only (build path); proxy pure Go.

## 2026-08-06 20:40 — Ideation: drop simplestreams proxy, push directly via Incus API
User questioned the proxy; agreed. Rationale: simplestreams strengths (anonymous pull-at-scale, cache auto-refresh, big catalogs) don't apply — bootstrap qcow2 is low-frequency, few images, day-2 = bootc. Direct path: incus Go client (github.com/lxc/incus/client), image upload = metadata+qcow2 streams, push over API from CI/laptop (works for IncusOS API-only hosts; cluster = one target), fingerprint makes sync idempotent, alias flip = rollout/rollback. Kills the only standing service. Honest losses: no remote-catalog launch UX, connectivity direction flips (publisher needs API ingress), no auto-refresh (moot w/ bootc). Distribution = port; Incus-API push adapter v1; simplestreams façade remains possible later over same OCI artifact contract (annotations+fingerprint stable).

## 2026-08-06 21:00 — Proxy debate resolved(?): sync-agent pattern
User narrowed proxy's pull advantage (proxy needs registry reach + limited auth: basic/mTLS; local proxy ≈ local CLI; genuine differentiator = IncusOS which can run neither... except privileged container). KEY CORRECTION delivered: privileges belong to BUILD only (podman/loop devs in CI); sync path is unprivileged (registry egress + API token) → can run as ordinary Incus instance ON the IncusOS cluster = "sync-agent mode". Preserves pull properties (egress-only, fleet-membership decoupling via per-cluster config) + full OCI registry auth (beats proxy's basic/mTLS ceiling), no standing HTTPS service. Proxy uniquely useful ONLY for consumers outside administrative reach (anonymous/public catalogs, native remote: UX) — deferrable as pure façade over same OCI artifact contract. Recommendation: build publish + sync (agent-mode-capable config from day 1); proxy waits for a concrete external-consumer story.

## 2026-08-06 21:15 — Renamed: incus-oci → incus-bootc
Design synopsis settled (publish: Containerfile → bootc qcow2 → OCI registry; sync: registry → Incus API import, agent-mode capable). Renamed before touching template code: `incus-oci` collided with Incus's own OCI-container feature vocabulary; bootc coupling is load-bearing. gh repo rename (redirect left), local dir moved to ~/code/componere/incus-bootc (symlink at old path for current session), remote set-url, worktrees repaired, memory dir migrated. go.mod still github.com/meigma/template-go — module path fix belongs to template initialization, not the rename.

## 2026-08-06 21:45 — Design document written
DESIGN.md drafted in .journal/001/ per user request: full design incorporating CLAUDE.md rules (hexagonal ports/adapters w/ rule citations, three test layers, mockery, streaming, sentinels/retries), Plain Language style with three passes (draft → tell-scan/word-level edits → structural pass fixing overbroad §6 claim, Registry port ownership split, diagram). All resolvable questions answered inline (target-imgref ordering, referrers discovery w/ oras fallback, disk-size default vs growpart, fs selection precedence, retention=2, requirements.secureboot in metadata [verify], e2e-on-GH-runners incl. KVM). One open question reserved for user: signing posture (lean: sign-if-key-present).

## 2026-08-06 23:00 — Spike 2: factory pattern PROVEN E2E
Chain proven: factory bootc image (fedora-bootc:44 + jq + factory-switch oneshot: ImportCredential=target, bootc switch --apply w/ 3 retries, guard on booted==target) → bootc install to-disk → qcow2 → metadata → incus-simplestreams tree → nginx TLS (self-signed CA in system trust) → `incus remote add factory https://127.0.0.1:8444 --protocol simplestreams` → `incus launch factory:fedora/44/factory factory-vm --config 'raw.qemu=-smbios type=11,value=io.systemd.credential:target=10.171.99.1:5000/spike-bootc:spike'` → pid1 logs "Received regular credentials: target" → service switches, auto-reboots → VM booted target: marker V2, bootc booted==target ref, rollback==factory image, factory-switch.service ABSENT in target (structural idempotency). T+795s from launch to verified switch (nested virt; post-switch first boot slow at ostree-prepare-root).
FINDINGS vs design: (1) `requirements.secureboot: "false"` in simplestreams properties does NOT auto-disable secure boot — VM hit shim 0x1A; explicit security.secureboot=false still required → DESIGN §6 assumption fails verification, update. (2) incusd loads CA trust at startup — restart required after update-ca-certificates (client-side simplestreams reads worked pre-restart; server-side image fetch failed until restart). (3) SMBIOS credential redelivered every boot; harmless post-switch since unit gone.
GOTCHAS: watcher scripts silently empty (ssh no-route during boot cycles; console ring resets per boot obscures history). Negative test (no credential) in flight: factory-neg.

## 2026-08-06 23:10 — Spike 2 closed
Negative test passed: factory-neg stayed factory ("no 'target' credential" logged, marker unchanged). Wakeup loop stopped. DESIGN.md §6 corrected (requirements.secureboot documentation-only; explicit security.secureboot=false required; optional sync-managed profile deferred). FINDINGS.md addendum + spike2 sources committed to journal.

## 2026-08-07 00:15 — GitHub Pages as simplestreams: PROVEN feasible (with Releases for blobs)
Four empirical results (lab, Incus 7.3): (1) compressed qcow2 (qemu-img -c: 2.1GB→988MB) imports and unpacks fine; (2) simplestreams item paths accept ABSOLUTE URLs (fetched blob from different host:port than index); (3) client follows 302 redirects (nginx redirect chain mimicking GitHub Releases → objects.githubusercontent.com); (4) base URL with subpath works (`remote add https://host/incus-bootc` — matches project Pages layout). Architecture: Pages hosts streams/v1/*.json + tiny metadata tarballs (well under 100MB/1GB caps); qcow2 blobs as Release assets (≤2GiB each, free public bandwidth), images.json paths = absolute browser_download_urls. incus-simplestreams tool can't emit absolute paths — incus-bootc generates the JSON itself (static generation; NOT the standing proxy we rejected — this covers the public/anonymous-consumer case with zero running services). CI: build → upload release assets → regenerate JSON → push Pages branch.

## 2026-08-07 08:45 — incus publish as template mechanism: proven, with hygiene trap
`incus publish factory-vm --alias app-template` (stopped VM): 52s, 999MiB compressed unified image. Clone boots DIRECTLY into target (marker V2, tracking registry ref, no switch — unit absent). TRAP CONFIRMED: clone /etc/machine-id identical to original (67453580c2fc...), inherited journal (old factory-switch entries visible), host keys, /var. Publish = snapshot, not sysprep. Pattern requires prep before publish: truncate machine-id, rm ssh host keys, vacuum journal, optionally undeploy factory rollback. Positioning: switch tax only exists in factory mode; incus-bootc publish already yields switch-free identity-clean disks. Template pattern = docs recipe for factory-only consumers (no build infra), not tool machinery.
